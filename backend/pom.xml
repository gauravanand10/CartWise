<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.cartwise</groupId>
	<artifactId>cartwise-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>cartwise-backend</name>
	<description>CartWise backend service</description>

	<properties>
		<java.version>21</java.version>
	</properties>

	<!--
		Chapter 15 deliberately keeps the dependency set minimal.

		Not present, and why:
		  - No spring-boot-starter-data-jpa / PostgreSQL driver  -> Chapter 16 (Database Design)
		  - No spring-boot-starter-security                      -> Chapter 18 (Authentication)
		Adding either now would mean configuring a datasource or a filter chain that
		nothing in this chapter actually uses.
	-->
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
