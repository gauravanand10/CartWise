import { WishlistPage } from "../../features/wishlist";

/** Route entry for /wishlist. The feature owns the implementation. */
function Wishlist() {
    return <WishlistPage />;
}

export default Wishlist;
